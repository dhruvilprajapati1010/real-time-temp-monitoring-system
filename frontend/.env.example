# frontend/.env
# Copy this to frontend/.env and adjust.
# Vite exposes only variables prefixed with VITE_ to the browser.

# URL of your Flask backend (no trailing slash)
VITE_API_URL=http://localhost:5000
